# src/searchEventHandlers.js

This file wires the search input to the application behavior.

## What it does
- Adds an input event listener to the search field.
- Calls the search handler each time the user types.

## Role in the app
It makes the search interface interactive without requiring any inline event handlers in the HTML.
